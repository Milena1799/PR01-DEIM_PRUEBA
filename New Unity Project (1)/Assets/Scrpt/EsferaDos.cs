using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EsferaDos : MonoBehaviour
{
    int myNum = 5;
   

    // Start is called before the first frame update
    void Start()
    {
        while (myNum >=0)
        {
            Debug.Log(myNum);
            myNum--;
        }

      

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
